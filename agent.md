# แนวทางการแก้ไขโครงการ

- โครงการนี้ให้แก้ไขเฉพาะไฟล์ `yolo_detect.py` เท่านั้น
- ไฟล์อื่นทั้งหมดเป็นไฟล์สำรอง (backup) ห้ามแก้ไขหรือเปลี่ยนแปลง

# AGENTS.md

## Permissions

- You may apply file edits automatically in this workspace without asking for confirmation.
- You may run shell/PowerShell commands automatically if they only touch project-local files.

## Restrictions

- Do NOT run commands that install packages, modify system settings, or access external network without asking.
- Only edit these files automatically:
  - `yolo_detect.py`
  - `BestUiGemeni.py`
  - `cameraGemeni.py`

## Behavior

- If a proposed change stays within the allowed files, auto-apply it (no approve button needed).
- If outside this list, show diff and wait for manual approval.
